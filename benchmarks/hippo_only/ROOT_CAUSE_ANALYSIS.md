# 근본 원인 분석 (Root Cause Analysis)

**Date**: 2026-01-20  
**Version**: v0.4.0-alpha

---

## 🔴 핵심 문제 발견

### 문제 1: 보정값이 모두 0.0

**증거**:
```
보정값이 0이 아닌 경우: 0
최대 보정값: 0.0
평균 보정값: 0.0
```

**원인**: 편향이 저장되지 않음

---

### 문제 2: 편향 저장 조건이 너무 엄격함

**현재 조건**:
```python
if abs(error) < 0.01:  # 안정 구간
    memory.store(...)
```

**문제**:
- PID 제어기로는 `abs(error) < 0.01` 조건이 거의 만족되지 않음
- 실제 에러 분포: 평균 0.48, 최대 0.37
- 조건 만족률: **0%** (거의 모든 경우에서 조건 불만족)

**결과**: 편향이 저장되지 않아서 보정값이 항상 0.0

---

## 🔍 상세 분석

### 1. 에러 분포 확인 필요

실제 에러 분포를 확인하여 적절한 저장 조건을 설정해야 함.

**예상 결과**:
- 평균 에러: 0.4~0.5 수준
- `abs(error) < 0.01` 만족률: 0~5%
- `abs(error) < 0.1` 만족률: 10~20%
- `abs(error) < 0.5` 만족률: 80~90%

---

### 2. 해결 방안

#### 방안 1: 저장 조건 완화 (권장)

```python
# 현재
if abs(error) < 0.01:

# 수정 1: 조건 완화
if abs(error) < 0.5:  # 대부분의 경우 저장

# 수정 2: 항상 저장 (학습 게이트 사용)
memory.store(...)  # 조건 제거, Learning Gate가 제어
```

#### 방안 2: 다른 조건 사용

```python
# 속도 기반 조건
if abs(velocity) < threshold:

# 가속도 기반 조건
if abs(acceleration) < threshold:

# 목표 근접 조건
if abs(target - current_pos) < threshold:
```

#### 방안 3: 드리프트 크기 증가

```python
# 현재
drift_rate = 0.0002

# 수정
drift_rate = 0.001  # 5배 증가
# → 에러가 더 커져서 저장 조건 만족 가능성 증가
```

---

## 🎯 즉시 적용 가능한 수정

### 수정 1: 저장 조건 완화

```python
# benchmarks/hippo_only/benchmark_drift_before_after.py
# Line 182

# 현재
if abs(error) < 0.01:

# 수정
if abs(error) < 0.5:  # 조건 완화
```

### 수정 2: 드리프트 크기 증가

```python
# benchmarks/hippo_only/benchmark_drift_before_after.py
# Line 230

# 현재
drift_rate = 0.0002

# 수정
drift_rate = 0.001  # 5배 증가
```

### 수정 3: 위치 간격 증가 (선택)

```python
# benchmarks/hippo_only/benchmark_drift_before_after.py
# Line 228

# 현재
target_positions = [0.0, 0.1, 0.2, 0.1, 0.0]

# 수정
target_positions = [0.0, 1.0, 2.0, 1.0, 0.0]  # 10배 증가
```

---

## 📊 예상 개선 효과

위 수정 사항 적용 시:

1. **저장 조건 완화** → 편향 저장률 0% → 80~90%
2. **드리프트 크기 증가** → 학습 가능한 편향 크기 증가
3. **위치 간격 증가** → Place ID 분리 명확화

**예상 결과**:
```
[Baseline PID]
  Drift RMS: 0.XXX
  Final Error: 0.XXX

[PID + Hippo Memory]
  Drift RMS: 0.XXX  (-20~40%)
  Final Error: 0.XXX (-20~40%)
```

---

## ✅ 검증 계획

1. ✅ 문제 진단 완료
2. 🔄 수정 사항 적용
3. 🔄 재벤치마크 실행
4. 🔄 보정값 확인 (0이 아닌 값이 나와야 함)
5. 🔄 개선율 확인 (20% 이상 개선 목표)

---

**Author**: GNJz  
**Made in**: GNJz  
**License**: MIT License

